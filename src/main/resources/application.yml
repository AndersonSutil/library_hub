server:
  port: 8080

spring:
  application:
    name: library-hub
  data:
    mongodb:
      uri: mongodb://localhost:27017/libraryhub
    redis:
      host: localhost
      port: 6379
  cache:
    type: redis

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
